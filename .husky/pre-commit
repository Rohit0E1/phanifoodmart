echo "✅ Running pre-commit: formatting & linting staged files"

# Format only staged files
npm run format:staged || exit 1

# Run lint (with auto-fix if possible)
npm run lint || exit 1

echo "✅ Pre-commit checks passed!"